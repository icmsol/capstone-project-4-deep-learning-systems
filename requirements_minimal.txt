# Portable project-relevant dependencies for local execution.
# The exact final Google Colab environment snapshot is stored in requirements.txt.
# For GPU acceleration, install the PyTorch build appropriate for the local OS,
# GPU driver, and CUDA environment.
jupyterlab>=4,<5
matplotlib>=3.8,<4
numpy>=2.0,<3
pandas>=2.2,<3
scikit-learn>=1.6,<2
torch>=2.5,<3
